[package]
name = "limousine_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
limousine_engine = { path = "../engine" }

[dev-dependencies]
rand = "0.8.5"
rand_distr = "0.4.3"
tempfile = "3.0"
